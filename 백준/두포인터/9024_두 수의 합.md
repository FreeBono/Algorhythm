```python
import sys
from itertools import combinations
sys.stdin = open('input.txt')

t = int(input())
for _ in range(t):
    n, k = map(int,input().split())
    ls = list(map(int,input().split()))
    ls.sort()
    print(ls,k)
    start = 0 
    end = n-1
    ans = 0
    while start <= end:
        diff = k - ls[start] + ls[end]
        
```

