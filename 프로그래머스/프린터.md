# 1. 프린터

```python
from collections import deque

def solution(priorities, location):
    answer = 0
    priorities = deque(priorities)
    for i in range(len(priorities)):
        if i == location:
            priorities[i] = [priorities[i],1]
        else:
            priorities[i] = [priorities[i],0]
        
    
    
    while True:
        maxx = max(map(max,priorities))
        
        if priorities[0][0] == maxx:
            answer += 1
            t = priorities.popleft()
            
            if t[1] == 1:
                break

        else:
            t = priorities.popleft()
            priorities.append(t)
        

    return answer
```

