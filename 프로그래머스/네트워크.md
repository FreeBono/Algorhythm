# 네트워크

```python
def solution(n, computers):
    def dfs(x):
        visited[x] = 1
        for w in range(n):
            if computers[x][w] == 1 and visited[w] == 0:
                dfs(w)
    answer = 0
    visited = [0]*n
    cnt = 0
    
    for i in range(n):
        for j in range(n):
            if computers[i][j] == 1 and visited[i] == 0:
                answer += 1
                dfs(i)
                
    return answer
```

전형적인 bfs문제.

어렵지 않았다.

